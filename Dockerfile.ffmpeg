FROM alpine:3.20

# ffmpeg with libx264 support for transcoding H.265 → H.264
# (Xibo's CEF player cannot decode H.265)
RUN apk add --no-cache ffmpeg x264

COPY entrypoint-ffmpeg.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh

ENTRYPOINT ["/entrypoint.sh"]