# hls-restreamer

This docker setup allows you to bypass rai iptv streams relinker cdn and geoblocking.

If you're using Pangolin proxy, enable Enable Sticky Sessions.

Run:
docker compose build && docker compose up -d.