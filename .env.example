# ─── RAI Restream — environment overrides ───────────────────
# Copy this to .env or export these before running docker compose.

# The RAI relinker URL (change cont= for a different channel/stream)
STREAM_URL=https://mediapolis.rai.it/relinker/relinkerServlet.htm?cont=358025&output=16

# Stream quality: best | worst | or any specific label (e.g. 720p)
STREAM_QUALITY=best

# Port exposed on your host for the HLS endpoint
HLS_PORT=8080